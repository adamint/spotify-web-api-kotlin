/* Spotify Web API, Kotlin Wrapper; MIT License, 2017-2021; Original author: Adam Ratzman */
package com.adamratzman.spotify.webplayer

/*
class WebPlaybackSdkTestK: FunSpec({
    test("test") {
        println("test ran")
    }
})

class WebPlaybackSdkTest {
    fun testCreateDevice() {
        GlobalScope.launch {
            loadSpotifyJs()
            delay(10000)
            println("l")

            val player = SpotifyPlayer(
                object : PlayerInit {
                    override var name: String = "Test Player"
                    override fun getOAuthToken(cb: (token: String) -> Unit) {
                        cb(api.token.accessToken)
                    }
                }
            )
            player.connect().then { success ->
                println("Success? $success")
            }
        }
        val time = getCurrentTimeMs()
        // while (getCurrentTimeMs() - time < 50000) {}

    }
}
*/
